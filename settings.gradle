pluginManagement {
    repositories {
        google()
        mavenCentral()
        jcenter() // If you need to set chart, include this repository
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // If you need to set chart, include this repository
        maven { url 'https://jitpack.io' } // If you need to set chart, include this repository
    }
}

rootProject.name = "Final Money Manager"
include ':app'
