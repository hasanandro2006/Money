pluginManagement {
    repositories {
        google()
        mavenCentral()
        jcenter()// if set chart set this
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()// if set chart set this
        maven { url 'https://jitpack.io' }// if set chart set this
    }
}
rootProject.name = "Final Money Manager"
include ':app'


